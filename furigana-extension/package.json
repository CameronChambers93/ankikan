{
  "name": "furigana-extension",
  "version": "1.0.0",
  "description": "AnkiKan browser extension",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitest/coverage-v8": "^2.0.0",
    "jsdom": "^25.0.0",
    "vitest": "^2.0.0"
  }
}
